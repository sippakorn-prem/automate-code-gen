version: 2.1

workflows:
  set-env-vars:
    jobs:
      - build:
          context: Code Gen
jobs:
  build:
    docker:
      - image: ubuntu:16.04
    steps:
        - checkout
        - run: 
            name: "echo an env var that is part of our context"
            command: |
              echo $APP_ID
